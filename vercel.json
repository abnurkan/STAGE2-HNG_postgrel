{
    "builds": [{
        "src": "auth_system/wsgi.py",
        "use": "@ardnt/python",
        "config": { "maxLambdaSize": "15mb", "runtime":"python3.9" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "auth_system/wsgi.py"
        }
    ]
}